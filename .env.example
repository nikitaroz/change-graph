# Optional. Copy to `.env.local` (gitignored) and restart `npm run dev`.
# Classic PAT: https://github.com/settings/tokens/new?description=change-graph
# No scopes needed for public repositories. Do not commit a real token.
# VITE_GITHUB_TOKEN=ghp_your_token_here
